from django.urls import path

# Transport.finance will provide financial management endpoints
# These can be expanded with actual views later
urlpatterns = [
    # path("", FinancialListView.as_view(), name="transport-finance-list"),
    # path("<int:pk>/", FinancialDetailView.as_view(), name="transport-finance-detail"),
    # Add more finance endpoints as needed
]