from django.urls import path

# Transport.reports will provide reporting endpoints
# These can be expanded with actual views later
urlpatterns = [
    # path("", ReportListView.as_view(), name="transport-reports-list"),
    # path("", ReportDetailView.as_view(), name="transport-report-detail"),
    # Add more report endpoints as needed
]