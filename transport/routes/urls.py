from django.urls import path

# Transport.routes will provide route management endpoints
# These can be expanded with actual views later
urlpatterns = [
    # path("", RouteListView.as_view(), name="transport-routes-list"),
    # path("<int:pk>/", RouteDetailView.as_view(), name="transport-route-detail"),
    # Add more route endpoints as needed
]