from django.urls import path

# Transport.fuel will provide fuel management endpoints
# These can be expanded with actual views later
urlpatterns = [
    # path("", FuelEntryListView.as_view(), name="transport-fuel-list"),
    # path("<int:pk>/", FuelEntryDetailView.as_view(), name="transport-fuel-detail"),
    # Add more fuel endpoints as needed
]